{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build-prod": "vite build",
    "build-uat": "vite build --mode staging",
    "preview": "vite preview",
    "test": "npx mocha test/*.test.js --timeout 6000"
  },
  "license": "ISC",
  "dependencies": {
    "assert": "^2.1.0",
    "dotenv": "^16.4.5",
    "selenium-webdriver": "^4.18.1"
  },
  "devDependencies": {
    "vite": "^5.1.4",
    "mocha": "^10.2.0"
  }
}
